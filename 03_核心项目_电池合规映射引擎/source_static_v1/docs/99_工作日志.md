# 工作日志

## 2026-04-07（下午）

### 本轮目标

在原有静态 demo 基础上，创建增强版前端，增加交互功能用于比赛展示和团队审查。

### 本轮完成内容

1. **增强版前端文件**：
   - `frontend/index-enhanced.html` - 增强版入口页面
   - `frontend/styles-enhanced.css` - 增强版样式
   - `frontend/app-enhanced.js` - 增强版交互逻辑

2. **新增交互功能**：
   - 角色切换（查看者/审核者/管理员三种视角）
   - 视图切换（表格/卡片两种展示模式）
   - 字段筛选（全部/可直接引用/需要拆分/存在缺口）
   - 证据链展示（弹窗形式展示完整数据追溯路径）
   - 四层映射展开/收起

3. **补充文档**：
   - `docs/04_增强版功能说明.md` - 功能使用说明
   - `docs/05_组件说明.md` - 技术组件说明

### 本轮关键判断

1. 增强版与原版并行，不影响原有展示逻辑
2. 保持零新增外部依赖，轻量化优先
3. 角色权限通过配置对象管理，便于后续扩展

### 后续建议

1. 优先把 `demo-data.js` 和 `review_csv` 做字段对齐
2. 再把手动输入和输出做联动
3. 再考虑切到 React / API
4. 方法论、字段口径、合规边界继续由主模型把关

---

## 2026-04-07（上午）

### 本轮目标

把已经完成的 CATL 四层映射 demo 整理成一个可交接、可继续开发、可放到 GitHub 私有仓库中的前端项目。

### 本轮完成内容

1. 新建了独立项目目录：
   `D:\VibeCoding\codex\github_private\catl-four-layer-mapping-demo-20260407`
2. 建好了零依赖静态前端：
   - `frontend/index.html`
   - `frontend/styles.css`
   - `frontend/app.js`
   - `frontend/data/demo-data.js`
3. 补齐了给同学和后续 AI 的说明文档：
   - `docs/00_给同学看的介绍.md`
   - `docs/01_开发说明.md`
   - `docs/02_后续开发建议.md`
   - `docs/03_AI协作边界.md`
4. 把当前 demo 的工作簿纳入仓库：
   - `assets/workbooks/catl_four_layer_mapping_engine_demo.xlsx`
   - `assets/workbooks/catl_four_layer_mapping_engine_demo_with_public_arch.xlsx`
5. 把团队审查用 CSV 纳入仓库：
   - `data/review_csv/01_手动输入_review.csv`
   - `data/review_csv/02_欧盟电池法输出_review.csv`
   - `data/review_csv/03_CBAM输出_review.csv`
   - `data/review_csv/04_字段映射过程_review.csv`
   - `data/review_csv/05_证据映射过程_review.csv`
   - `data/review_csv/06_核验映射过程_review.csv`
   - `data/review_csv/07_权限映射过程_review.csv`
   - `data/review_csv/08_生命周期计算过程_review.csv`
   - `data/review_csv/11_CATL公开披露架构_review.csv`
6. 把关键中文底稿和参考说明纳入仓库：
   - `assets/reference_notes/*.md`
   - `assets/reference_notes/*.csv`
7. 把本轮 demo 主要参考的 CATL 官方公开资料纳入仓库

### 本轮关键判断

1. 当前最适合先做成静态前端，而不是一开始就上复杂框架。
2. 另一个 AI 更适合负责实现层，核心推理和合规口径仍应由主模型继续控制。
3. 仓库必须把”代码层””数据层””说明层””参考层”分开，否则后续很容易改乱。
4. 这个 demo 的核心不是炫技，而是把以下主线讲清楚：
   - CATL 已经有数据
   - 但现有很多是公司级
   - 欧盟电池法规要看产品级 + 工厂级
   - 上游材料还需要兼容 CBAM 风格字段
   - 所以必须做四层映射

### 后续建议

1. 优先把 `demo-data.js` 和 `review_csv` 做真实字段对齐。
2. 再把手动输入和输出做联动。
3. 再考虑切到 React / API。
4. 方法论、字段口径、合规边界继续由主模型把关。
